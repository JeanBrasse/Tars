export interface Release {
  id: number;
  version: string;
  date: string;
  updates: string[];
}

export const CHANGELOG: Release[] = [
  {
    id: 49,
    version: '1.9.0',
    date: '2026-09-24',
    updates: [
      'The Chat is redrawn. Hermes and every project\'s room share one list on the left, with what waits in each room said by count: who needs you, what is not sent or held, what is running, queued or stopped. The open room shows its thread first, its team listed under the rooms on the left, and above the thread a strip of what needs you, each row with its button: open the terminal, send a message that was not sent, or start a stopped agent',
      'Your own lines in a room say who has them and who does not, what went wrong first: dropped, not sent, held, queued, then delivered, with the names; a long list is summed up as "and 25 more". A change of members is its own line, "You removed QA from the room.", and says when it closed the exchange and what was dropped with it',
      'A message for an agent that is busy waits for its turn to end, and the composer says so before you send. For an agent running Claude, send now interrupts the turn and delivers at once, after asking you, and the room records that you interrupted it; when it could only queue the message, the composer says that instead. A message behind something typed in the agent\'s own field is held until that is sent or cleared, and says so',
      'Files dropped on a room, pasted or picked with + go with your message, each saved where every agent can read it; one over 12 MB is refused with its name and the limit. A failure the composer shows, such as a refused file or an agent that did not start, goes away as soon as you type again or pick another recipient',
      'An agent in a room says since when it is in its state, "running 1m", what it waits on, a permission dialog or a question for you, and that it is starting while its session comes up. The head of a room says whether an exchange is under way and how many agent messages remain before it pauses, and, with none open, how many agents are still at work',
      'The Chat\'s menus work from the keyboard: Enter opens one on its first item, the arrows move, Escape closes it and returns to its button, and a new message no longer closes a menu you have open. When the bus does not answer, the room list and the room say so with a retry, and the composer is off until it does',
      'A room whose project sits at a long path keeps its name whole in the room\'s head and cuts the path instead, and how it runs stays on one line, where the name shrank to its first letters and the button wrapped out of its box',
      'Tars has a Discord bot, beside Telegram and Slack, with the same commands, and the orchestrator answers there. Settings > Discord takes its token, copies the invite link made from it, shows the channel the bot detects when you mention it or write to it, and lists the members it answers; anyone else who writes to it directly or mentions it is told their Discord ID. In a server it reads only messages that mention it, unless you turn that off, and every message it posts tells Discord to notify nobody, even one where an agent wrote @everyone',
      'The invite asks Discord only to see channels and send messages, in channels, threads and direct messages. It asked to read the channels\' history too, which the bot never does. When the token you type holds no bot id, Settings > Discord says so, where it asked you to set the token first',
      'Importing a template shows everything it sets before anything is saved: its permissions, with Skip all checks in red and named again above the buttons, the folders it adds, its skills and its whole prompt, with any character that does not show written out and counted. A file Tars cannot show as it will be used is refused whole. Using a template sends its prompt only when you turn that on, and that starts off unless the template is a built-in one left as it ships, since an imported template looks like one you made and an edit is kept where any agent can write',
      'Stopping or deleting an agent ends the task it was delegated, and every command its CLI started, even when that CLI no longer answers; the one who delegated it is told it was stopped. Quitting Tars ends them too, within about a second and a half, where a stuck one kept running after Tars had closed',
      'The agents\' memory search no longer returns your conversations with Hermes in the Chat. Your own Brain page still finds everything',
      'Telegram\'s /auth refuses a chat after five wrong tokens in fifteen minutes, and every new chat after twenty from all chats, and says at what time it can try again, or that turning Telegram off and on in Settings starts the count again',
      '~/.dorothy is closed to the other accounts on your Mac at every start, its folders and files readable by you alone. Tars never marks your home folder, / or a folder above your home as trusted for Claude, however it is spelled, and the hooks accept only a real session id',
      'Tars opens its own pages in the menu bar panel whatever NODE_ENV your shell sets. Launched from a shell with NODE_ENV=development, the panel loaded whatever answered on localhost:3000, with Tars\'s own bridge',
      'An Escape that ends an agent\'s turn ends it in Tars too: the agent reads idle again and what waited for its rest goes in, where it stayed running until its next turn. A turn resumed on the same conversation is not ended by an interruption it copied from before',
      'An agent may delete only a kanban task it filed or claimed, and a damaged Hermes connection file no longer sends the kanban to the default address',
      'A project reached through a symbolic link keeps its restart waiting for the work its session left running, and the Chat reads its agent\'s conversation',
      'An orchestrator waiting on an agent that is stopped or restarted before its background work reported is told so within seconds, instead of waiting for a message that never comes',
      'When Tars does not answer one of its tools in time, the tool says "Tars did not answer" and after how long, such as "no answer within 30 s", where it said "This operation was aborted"',
    ],
  },
  {
    id: 48,
    version: '1.8.1',
    date: '2026-09-24',
    updates: [
      'Nothing Tars types answers a permission or question dialog that an agent\'s Claude is showing. A Chat message, a delegation note, or a message from Telegram or Slack waits while the dialog is up, and goes in once you have answered or refused it. Measured with a real Claude before this: a room post answered "Yes, delete it" to a question, bypass mode included, and said Yes to a Bash command, which ran',
      'The agents\' kanban is the Hermes board, the one the Kanban page shows. A task an agent files arrives parked, where Hermes never starts it, and a Tars agent of that project claims it, one agent at a time. Hermes works on a task only once you give it a Hermes profile and move it to ready on the Kanban page. The open tasks of the old local board moved to the Hermes board once, parked on their projects, and the old file stays as a backup. With no Hermes connection, or Hermes not answering, the kanban tools say so and write nothing',
      'In the Chat, what you were typing in one room no longer follows you into the next, where Enter sent it to that room\'s agents; it is dropped when you leave the room. An agent on a CLI that never reports its turns, such as grok, shows in the room\'s team why it stopped on an error, where it read "no turn signal"',
      'In a busy Chat room, the older messages can be scrolled back to. Only the last five or so were in reach, and the wheel moved nothing. A room still opens at its newest message',
      'Run from source on Linux: the open button beside the config folder in Settings > System opens a terminal there, the first installed of x-terminal-emulator, gnome-terminal, konsole and xterm, where it did nothing; the quick terminal and the other terminals Tars opens outside an agent run bash when SHELL is unset, where they failed without zsh; and the status line reuses its git cache, as on macOS',
    ],
  },
  {
    id: 47,
    version: '1.8.0',
    date: '2026-09-24',
    updates: [
      'An agent launches on the model and effort you set for it, and a change of model, effort or permission mode takes effect by itself: Tars restarts its CLI on the same conversation once it is between turns, never during a turn, a permission question or something you are typing. A relaunch no longer comes back on the model its last session happened to use. This covers Claude and the providers that run on it; another CLI takes the change at its next start',
      'When a changed setting is waiting to restart an agent, its Dashboard panel says so and what the restart waits for, such as the end of the turn in progress',
      'A message to an agent whose CLI is running is typed into its session. Once a turn had ended, a message sent through Tars killed that session and started a fresh one that remembered nothing, and whatever you had typed in its field went with it; it happened to an orchestrator\'s own session. A message sent with the Tars tools to a terminal where no CLI runs is not typed into the shell: Tars starts the agent there instead',
      'A message sent while an agent\'s CLI is still starting waits for it, then goes into that session, and keeps the conversation being resumed. On a loaded machine, a tool that sends it waits up to 20 seconds, then is told that nothing was typed and to send it again; the Telegram and Slack bots wait up to three minutes',
      'A message waiting behind a /model or /effort picker you answered by hand goes in by itself once the command finishes, where it used to wait until you pressed Ctrl+C. The Tars tools say HELD rather than Sent when a message waits, and every message Tars types into a terminal first names who sent it: an agent, Tars, Telegram, Slack or Hermes',
      'A task delegated to an agent that reaches its limits is reported with what it had done, instead of being lost: work it leaves running is reported when it ends, the orchestrator keeps listening while it waits, and the brief is never sent twice',
      'The Orchestrator switch is the agent\'s role. A project has one orchestrator: switching it on for another agent asks first, naming the current one, then hands the role over and restarts both once they are free. Renaming an agent never changes its role, switching the role leaves its permission mode alone, and an orchestrator starts on the permission mode set for it, where Tars always put it in bypass',
      'Each agent is drawn with a small mark made from its name, on the Agents page, the Dashboard and in the Chat, and an orchestrator\'s mark is orange. Beside it, an agent\'s state is a word in its colour. The character picker is gone',
      'An orchestrator\'s window lists the other projects\' orchestrators that are running, idle or stopped on an error, drawn in orange',
      'The Agents page groups agents by project, in the order of the Dashboard\'s tabs, with a picker at the right of the filters that shows one project at a time. The status counts follow the project on screen, a completed agent is counted under Idle as its card says, and the filter field finds a branch',
      'A Dashboard panel you come back to shows its terminal\'s whole screen again, where it could come back unreadable, and a new terminal starts at its panel\'s size',
      'A panel whose Claude left fullscreen without telling the terminal says so on a line under its header, since the wheel cannot scroll it there, and offers to read the conversation from its history or to restart it on the same conversation',
      'Looking at an agent that is not running no longer opens a shell in its terminal: its Dashboard panel says so, (Session idle) for an idle one, and the Chat no longer shows that shell\'s banner as the agent\'s last words',
      'Usage: the timeframe, now in the page header, drives every figure on the page except the budget panel, which stays month to date. Today\'s cost follows as it grows instead of freezing until a new session, Claude keeps its budget row when another provider spent more this month, and Opus 5.5 is named Opus 5.5',
      'Tars keeps claude and Amp up to date itself, one at a time, a few seconds after it opens and every half hour, so a model a new release brings reaches your agents without running claude update by hand. It checks only the CLIs your agents run, and the one Check for updates switch in Settings covers them and Tars itself, taking effect without a restart. Sessions already running are not touched, and each result is written to ~/.dorothy/cli-updates.log',
      'Settings, the new agent and new team dialogs, Review, Extensions and the project lists answer from what Tars already found. The login shell that finds your CLIs runs once per launch, and again when you press detect now in Settings > AI & Providers > CLI Paths or after Tars installs Google Workspace, instead of on every section and dialog; a branch\'s diff is reused until a file or a branch moves; the skills marketplace shows its last listing at once and refreshes it once it is an hour old',
      'Tars keeps what it compiles of its own code between launches. Measured on a packaged build, compiling at launch went from 181 to 441 ms to 79 to 101 ms in the main process, and from 59 to 129 ms to 2 or 3 ms in the window',
      'The Chat\'s composer is one card, like Claude\'s and ChatGPT\'s, in the rooms and with Hermes. Enter sends and Shift+Enter starts a new line, a picker chooses who the message is for, and a line across the top says when a message cannot go yet: a failed send, which keeps your text, a stopped agent, which you can start from there, or a busy one it will wait for',
      'After you change the Hermes connection, sign in or sign out, the Chat goes back to its live Hermes session instead of the slower path it kept until Tars restarted. The Hermes errors that showed [object Object], on the Schedules page, the model picker, a memory write and the Chat\'s effort picker, read as Hermes wrote them, and the Hermes page gives the webhook address on this Tars\'s own port',
      'Telegram starts an agent that is not running again, on /start_agent or a message to the orchestrator: both launched nothing and left the agent marked running. Telegram and Slack now type the launch once the new terminal\'s shell has printed and gone quiet, so a message or task of about 950 characters starts the agent, where one of that length started nothing from Telegram',
      'The hooks your agents\' CLIs report through now present that terminal\'s own key: a hook call with no key, the shared key, another agent\'s, or the key of a terminal that was stopped or replaced is refused. Their logs moved from /tmp to ~/.dorothy/logs, readable by you only, and the old ones in /tmp are deleted',
      'The Slack bot answers only the members allowed in Settings > Slack, nobody while that list is empty, and tells anyone it refuses their Slack ID. Removing a Telegram chat in Settings cuts it off at once, and a file in ~/.tars-private can no longer reach the vault under another spelling of its path or through a hard link',
      'The Posting switch in Settings > X (Twitter) is enforced: while it is off, which it is by default, the Tars X tools refuse to post, reply or delete',
      'Run from source, npm run dev listens on this Mac only, and the web build\'s routes that could run commands are gone',
      'Tars runs on Electron 44 (Chromium 152) and needs macOS 13 or later',
      'Extensions and skills get plain icons, and the install dialogs and the broadcast notice no longer blur what is behind them',
    ],
  },
  {
    id: 46,
    version: '1.7.9',
    date: '2026-09-21',
    updates: [
      'A terminal says when a message is waiting for its field. Since 1.7.8 a message meant for an agent waits rather than being typed across a half-written prompt, and when Tars cannot promise to put your draft back exactly as it was, it writes nothing at all. Only the person at that keyboard can end that wait, so the panel now says so: a line under the panel header on the Dashboard, and at the top of the terminal in the agent window, naming who is waiting and how many, and the two things that end it. It appears the moment a message starts waiting, in every window, including one you open afterwards, and it goes as soon as you send what you were writing or clear the field with Ctrl+C',
    ],
  },
  {
    id: 45,
    version: '1.7.8',
    date: '2026-09-21',
    updates: [
      'An orchestrator is told when an agent it handed work to has finished, at the moment it finishes, and is no longer told that a working agent is waiting. Claude Code reports an agent idle one minute after every rest, and until now that report was the only thing that ever told an orchestrator a delegated turn was over: one minute late, and again every later time that agent came back to rest for any other reason. One orchestrator was told an agent "is now waiting" at the very moment it had handed it new work, through the link of a delegation that had ended ninety minutes earlier. Tars now announces the end of the work it handed over, once, when it happens, names a permission prompt as the question it is instead of wording it like a result, and says nothing at all when an agent simply sits still. An agent given work a moment ago stays working on the Dashboard, on the Agents page and in the menu bar badge, with no desktop notice claiming it is waiting for you. And when the orchestrator is already waiting on that agent through wait_for_agent or delegate_task, it is told once, by the answer it was waiting for, instead of reading the same thing again in its terminal a moment later',
      'A message from an agent no longer goes out with the sentence you were in the middle of writing. Typing in an agent\'s terminal while a message arrived for it submitted both as one prompt, and keys typed in the fraction of a second before the submit keystroke landed inside the message. While you are typing, a message now waits; at your first pause Tars empties the field, sends the message on a line of its own, and types back what you had written exactly as it was, same characters, same caret, several lines included, and leaves it unsent. Keys you type while that happens are kept and put in afterwards, in order. When Tars cannot promise to give a draft back, which is what happens after a key it cannot follow such as recalling history, Tab completion, deleting by word, or a paste long enough to be folded away, it writes nothing at all rather than write across it: the message goes in as soon as you send what you were writing, or clear the field with Ctrl+C. A tool that sends a message into a terminal that is busy that way is told the message is waiting, and why, instead of being told it was sent',
      'In the Chat, a held message says delivered when it reaches the agent\'s terminal, not when Tars picks it up. One released into a terminal that still holds something you were typing stays queued in the conversation until it is really written, so a row that says delivered means the agent has it',
    ],
  },
  {
    id: 44,
    version: '1.7.7',
    date: '2026-09-18',
    updates: [
      'With Tars opened from the Dock, delegating a task to an agent no longer opens the "A JavaScript error occurred in the main process" window that said "spawn npx ENOENT". Opened that way, Tars has only the system\'s short PATH, where npx is not, and the delegation started npx with it: the error went unheard up to that window, and the delegation waited 90 seconds before the task was typed into the agent\'s terminal instead. Tars now looks for npx in the folders it already searches when it opens an agent\'s terminal: those set in Settings > CLI Paths first, then ~/.nvm, /usr/local/bin, /opt/homebrew/bin and ~/.local/bin. When the command is in none of them, no window opens and the task goes to the agent\'s terminal at once.',
      'Three rarer cases could open the same window and no longer do: an agent that stops reading in the middle of a delegated task (its run ends at once and Tars stops it), a photo sent to the Telegram bot that Tars cannot save (the chat is told the download failed), and a vault attachment Tars may not open (Tars answers with an error).',
    ],
  },
  {
    id: 43,
    version: '1.7.6',
    date: '2026-09-18',
    updates: [
      'A Hermes job that gives your agents work through Tars\'s webhook now needs the webhook secret shown in Settings > Hermes. Up to 1.7.5, Tars turned that secret away before the webhook ever ran, so the jobs that worked were sending the key from ~/.dorothy/api-token, which every agent can read, and that key now gets a 403 there. Put the secret from Settings > Hermes in the job\'s Authorization header; if Settings already showed you one, it has not changed.',
      'An assistant session that Tars did not start as an agent can no longer start, stop, message, give work to, create or remove agents. The Tars tools are installed for every Claude Code session on the machine, so one you open in your own terminal, in a quick terminal or from a schedule sees them, but it can only present the key in ~/.dorothy/api-token, which every agent can read and which therefore says nothing about who is asking: it now gets a refusal. It can still list agents and read their output. Agents Tars starts, and the Chat, work as before.',
    ],
  },
  {
    id: 42,
    version: '1.7.5',
    date: '2026-09-18',
    updates: [
      'In the Chat, the model selector says when it cannot list models. With the Hermes gateway unreachable it looked like a control you could use; it is now greyed out, and pointing at it says the gateway did not list its models. When the gateway answers, nothing changes',
    ],
  },
  {
    id: 41,
    version: '1.7.4',
    date: '2026-09-17',
    updates: [
      'Tars no longer rewrites your Claude settings, ~/.claude.json or ~/.claude/mcp.json in place. Claude reads the first two each time it starts and the Claude sessions Tars starts read the third, yet Tars wrote into all three directly, and the status line rewrote the settings at every launch while it was on, even with nothing to change. A session reading at the wrong moment could meet a half-written file, and when Tars could not read the settings or mcp.json, it replaced the file with only the part it meant to add. These three files are now written beside the original and swapped in whole, keep their permissions, are left alone when nothing changes, and are never replaced when Tars cannot read them. The Kanban board file is also swapped in whole now, by Tars and by the agents\' Kanban server, so neither can leave it half-written',
      'A Dashboard panel shows stop while a CLI still runs in it. An agent at rest, or stopped on an error, keeps its Claude session open, and its panel offered start, which typed the launch command into Claude\'s prompt. The panel button and its menu now follow what runs in the terminal, and start types nothing into a terminal where a CLI is running. Once Claude has exited and only the shell is left, the panel offers start again',
      'A quick terminal you reopen after a lot of output still hands the wheel to the program and still pastes several lines as one. It replays only its last thousand writes, and the program\'s requests for the mouse and for marked pastes had fallen out of them, so the wheel sent nothing and a paste went out line by line. Those requests are now kept when the oldest writes are dropped',
      'Pages wait on the orange mark filling in, never on grey rows. Agents, Projects, Schedules, Review, Usage and the Skills tab showed grey placeholder rows while they loaded, and the history view of a Dashboard panel drew grey bars while it read the transcript. They now show the mark filling in over a line saying what is loading. Review, Logs and the tray panel no longer say they have nothing before they have had an answer, and the list of custom MCP servers in Settings waits on the mark instead of a line of text',
    ],
  },
  {
    id: 40,
    version: '1.7.3',
    date: '2026-09-17',
    updates: [
      'The wheel scrolls back through the conversation in a Claude Code terminal, in a Dashboard panel and in fullscreen alike. With Claude Code set to its fullscreen mode, the conversation is kept inside Claude Code and not in the terminal, so the terminal has no history of its own to scroll: since 1.7.2 the wheel no longer typed arrows there, but it did nothing else either. Claude Code scrolls that conversation itself when the terminal hands it the wheel, and Tars now does, the wheel and nothing more. Clicking, dragging to select text and Option+click stay with the terminal as before. A notch moves the conversation about as far as it moves any other terminal, a trackpad works too, a long session goes back to its first line, and nothing is typed into the prompt. Switching the panel to fullscreen and back leaves it readable and still scrolling. The agent window, the quick terminal and the tray work the same way. A program that has not asked for the mouse still gets nothing from the wheel, and a terminal that keeps its own history still scrolls it',
      'With Claude Code in its fullscreen mode, a Dashboard panel you reopen after a long turn, and the agent window, still pass Claude Code the wheel and still send a paste of several lines as one paste. Claude Code asks for the whole screen and for a paste marked where it begins and ends when it starts, and asks for the wheel again only when you type or the terminal is resized. Tars redraws these terminals from the recent part of what the agent printed, and after a few minutes of a turn with no input those requests were no longer in it, so a panel you reopened by switching pages and back stopped handing the wheel to Claude Code, and a paste of several lines went out as separate lines, each line break pressing Enter. Tars now keeps those requests when it trims what it keeps',
    ],
  },
  {
    id: 39,
    version: '1.7.2',
    date: '2026-09-16',
    updates: [
      'Scrolling a terminal no longer types into the agent. When a CLI fills the terminal, as Claude Code does, the terminal turned every line of wheel travel into a press of the up or down arrow and sent it to the CLI as if you had typed it. At Claude Code\'s prompt those arrows walk back through the messages you already sent, so scrolling up to reread the conversation filled the box with an old message, one Enter away from going out again. A mouse wheel and a trackpad did it alike, and a few notches of the wheel were dozens of arrow presses. The wheel now sends nothing to the program in that case, in every terminal in Tars: the Dashboard panels, the agent window, the quick terminal, the tray, the project shell and the install windows. A terminal that keeps its history still scrolls it with the wheel, and the arrow keys on your keyboard work as before',
    ],
  },
  {
    id: 38,
    version: '1.7.1',
    date: '2026-09-16',
    updates: [
      'An agent that stopped on a failure no longer tells you it is waiting for you. A minute after a turn fails, the assistant goes idle at its prompt exactly as it does after a turn that went well, and Tars turned that into the notification "is waiting for your input" for an agent that could do nothing until you logged it in again. The card already kept showing the failure; the notification now agrees with it. You still get the error notification with the assistant\'s own words, and once a new turn starts in that agent, waiting notifications come back as usual',
      'The Chat page no longer marks as dropped a message your agent read and answered. A message sent to an agent that was not busy reached its terminal at once, but Tars wrote down the delivery a moment after writing the message, so the mark that says delivered had nothing to land on: the message went on showing as queued, and turned dropped as soon as you wrote again, even though the agent had already replied. The delivery is now written down before the message goes out, so what the page shows is what happened. A message to an agent that is busy was never affected: it waits, and shows as delivered when the agent takes it',
      'An agent the super chat starts, gives a task to or stops now changes on the pages you already have open. The super chat drives agents through the same local address the assistants use, and that path changed the agent without telling the window, so the Agents page, the Dashboard and the list beside the Chat went on showing the old state until you left the page and came back. The same goes for an agent created, removed or given a delegated task that way, and for an agent whose process ends',
      'An agent you stop from the interface stays stopped. The assistant you stopped still sends a last word as it shuts down, saying its session has ended, and Tars accepted it as coming from the agent\'s current session, so an agent you had just stopped went back to showing as done a moment later. That last word is now recognised as coming from the session you ended, as it already was when an agent was stopped by another agent',
      'An agent that stops on an error now says why on every page that shows the error. Since 1.7.0 an agent whose turn fails carries the assistant\'s own sentence, such as "Not logged in · Please run /login", but the card on the Agents page went on showing the task it had been given, and the panel on the Dashboard showed a red square and nothing else, so the answer was in Tars and on neither of the pages you were looking at. The card now shows the reason where the task was, and the panel shows it in its header where the branch sits: one line, cut where the space runs out, with the whole sentence when you hover it. The team list in a Chat room had the same gap in another form, since it printed the reason only for an agent with no task, which an agent whose task has just failed never is. And the two pages were not being given the sentence at all: the notice that an agent changed status carries the new status and not the reason, so a page left open showed the error with nothing beside it, or with the reason for the failure before. They now read the agent again the moment it enters error',
      'A Chat room no longer calls agents at rest stopped. Claude Code reports idle at the end of every turn and keeps its session open, so a message to it is written at once, but the room read idle as stopped: two agents could be answering each other while the box under them said every agent here was stopped and that what you wrote would wait until you started one. It says stopped now only when Tars holds no live session for an agent, the team list marks that agent stopped with no live session instead of listening, and picking it says so beside the picker. The sentence also promised that a message would wait for the agent to start, which nothing did, so a room that really is stopped now only says that nothing moves until you start one',
      'An agent can no longer pass itself off as another just by giving a teammate\'s name. Every agent used to prove only that it was running on this machine, and then said which agent it was, so one agent could name a teammate and read that teammate\'s project conversation, or post in it under the teammate\'s name. Each agent now receives a secret of its own when Tars starts it, and so does a task delegated to it, and Tars goes by that secret rather than by any name. A call that only knows the secret shared by the whole machine is no agent at all, and enters no conversation. This stops impersonation by name, not every way in: agents run with your permissions, so an agent set on it can still read what you can',
      'A message you write to your agents in the Chat page now reaches them as yours. Every room message arrives in an agent\'s terminal with a note saying who wrote it, and that note said it came from a teammate and not from you, so that an agent never takes a colleague\'s request for your instruction. It said the same on your own messages, which told the agent the opposite of the truth. The note now follows who actually wrote the message, and an agent that happens to carry your name is still introduced as a teammate. And a message can no longer dress itself up as that note: Tars now marks exactly where each message starts and ends, with a mark drawn afresh every time that the message cannot know, so a line inside it that imitates the note stays visibly part of the message. Nor can an agent\'s name, or the path of its project, break the lines Tars writes around a message, or hide text in them: characters that start a new line or show as nothing now appear spelled out, in room messages and in the note that tells an orchestrator its agents have finished',
    ],
  },
  {
    id: 37,
    version: '1.7.0',
    date: '2026-09-16',
    updates: [
      'An agent whose turn fails no longer passes for one at work. When the assistant cannot run a turn, for example because it is not logged in, it writes the reason in its terminal and waits, but Tars was never told, so the agent went on showing as working for as long as you left it, and a whole fleet sharing one expired login went quiet together without a word. It now shows an error carrying the assistant\'s own sentence, such as "Not logged in · Please run /login", and the error notification says the same thing',
      'Two ways an agent could reach past its own work are closed. One let any agent read your private conversation with the super chat, which is yours and was never part of what an agent is shown. The other let a message sent to an agent stop being a message: it could arrive at the terminal of that agent in a form that made it run something instead of read it, which is the one thing the note attached to every message exists to prevent, since that note tells the agent it is hearing a teammate and not you. Neither is possible now, and nothing changes in how you use Tars',
      'Your agents keep reporting their status after you save settings. The Settings page reads the CLI configuration file once, when it opens, and sends the whole of that copy back when you press save. Tars writes the hooks your agents report through into that same file a moment after the window appears, so a Settings page opened in that moment holds a copy with no hooks in it, and goes on holding it for as long as it stays open. Pressing save then wrote that empty copy over the real one: eight hooks became none, and the permissions, the enabled plugins and the co-author setting went with them. It is one file for all fourteen of the assistants that run on the claude binary, so every agent went quiet at once, and nothing put it back until Tars was next started. Saving settings can no longer remove what the page never had',
      'The Usage page no longer freezes the window while it works out what you have spent. Pricing your usage means reading every transcript Claude Code has written, which on this machine is 1826 files and 884 megabytes, and it was done in one go on the same thread that draws the window and answers your clicks: for about three seconds the app was not slow, it was stopped, and asking for stats from Slack or Telegram stopped it in the same way. The reading now happens in small pieces that hand the window back many times a second, so the longest it holds on to anything is around a twentieth of a second. The figures come out identical to the last decimal. And a transcript that cannot be read is now counted and reported rather than quietly skipped, because a file that fails to open used to make the total look smaller instead of incomplete, which is the one kind of error that reads as good news',
      'Your Claude Code status line stops disappearing. Tars has a switch for its own status line, and on every launch it read that switch to decide whether to install its script or take it away. The switch had no value at all until you touched it, and an untouched switch was read as off, so every launch quietly deleted the status line entry from Claude Code\'s own settings file. It did that whether Tars had put it there or not, so a status line you had set up yourself was removed too, with nothing to say what had removed it. A switch nobody has touched is now a question nobody has answered, and Tars leaves that file alone. Turning the switch off removes only the status line Tars installed, never one pointing anywhere else',
      'An agent can no longer have its session taken from it. Everything an agent reports carries the identity of the session it came from, and that is how Tars knows which reports to trust: only the ones from the session it started. The identity is read out of the message by a small tool, and when that tool is missing the field comes back empty instead of failing, and empty was being read as no objection rather than as unusable. So a report carrying no identity at all passed every check. It could put a working agent to sleep, and at worst an agent announcing itself with an empty identity overwrote the record of who owns it and saved that to disk, after which any session could speak for that agent and everything the real one said was thrown away. The everyday version of this has been happening all along: stopping a terminal leaves scripts behind that post exactly such a report, and it quietly put the live session to sleep. Empty is now refused wherever that decision is made, the refusal names the field rather than blaming the session, and an agent whose record was already emptied repairs itself the next time it reports in',
      'Three ways in that were open wider than they looked are closed. One local address Tars listens on accepted a request with no token at all, marked any task on your board as done with a summary the caller wrote, and, if that task had created an agent, removed that agent from the running fleet while leaving its terminal behind. Nothing in Tars ever called it, so it is gone rather than guarded. A second one let a status report that named no session at all pass the check meant to ensure only the session that owns an agent can speak for it, and a status report is what makes Tars type a waiting note into a terminal, so that was a way to write into a turn already in progress. And a desktop alert could be raised for an agent that does not exist, carrying text the caller chose, under the name Claude. In each case the guard was written to check a value and quietly did nothing when the value was missing',
      'An agent no longer loses its session at the moment you give it work. Everything an agent reports is filed under the session that owns it, and handing that agent a task from the Agents page, from Telegram, from Slack or from the board wiped that record first, so that a safety check could ask whether a new session had started. For those four, no new session was ever going to start: the task is typed into the one already running. So the agent kept working while Tars no longer knew whose work it was, and everything it said was filed under nobody and thrown away, until some later report happened to claim it back. When none did, that same safety check went on to accuse it of never having started at all. The record is now left alone whenever the session holding it is genuinely alive, the check asks its question a different way, and a record left over from a session that died with an older terminal is still cleared, because that one really is stale',
      'Your agents get the task you gave them. A task handed to a CLI on the command line was being read as one more folder to grant access to: the option that mounts Tars\'s own instructions takes a list of folders, the task sat immediately behind it, and it was swallowed into that list. The agent came up with nothing to do, announced its session a second later, and every screen in Tars said it was working, which is why one could sit at an empty prompt for half an hour with nothing in its terminal and nothing on its branch. It applied to every start that carries a task: the Agents page, a dispatch from an orchestrator, Telegram, Slack and the Kanban board. The task is now passed where no option can take it, whatever flags come before it and whatever the CLI adds in a future version. An agent started with no task at all no longer spends a turn being told which skills it may use',
      'An agent that never takes its task says so in fifteen seconds rather than ten minutes, and is handed it a second time first. A session announcing itself used to count as proof that the task had arrived, which is exactly what a lost task looks like from the outside: the session is there, it is registered, and it is doing nothing. Tars now waits for the session to actually begin a turn. If none has begun fifteen seconds after it announces itself, the task is typed into the live session, the way a message from Telegram or Slack reaches an agent that is already running, and if that does not start a turn either the agent is marked failed with a note saying the task was sent twice and never ran. Its session is left open so you can take it over. Fifteen seconds because a session that did receive its task begins in about one, measured against the real CLI',
      'Your agents can hold a conversation, one room per project. Until now anything an agent had to say to another went through you, or through a message typed into a terminal at a moment nobody had checked was safe. There is a room for each project now, whose members are that project\'s agents, and the super chat you already use stays exactly as it is, watching every project and reporting across all of them. An agent speaks by choosing to: nothing it says at the end of a turn is published on its behalf, and having nothing to add costs nothing and is not shown. A conversation is bounded by code rather than by hope, three rounds and ten agent messages per thread, after which only you can reopen it, and after the first round only an agent that another one named gets a turn. Writing into the middle of a conversation changes nothing about the turn already running: your message is added, the turn finishes, and the discussion starts again from what you said',
      'A message to an agent is never quietly lost. Each one shows where it got to: queued while its target is busy, delivered the moment it actually reaches the terminal, dropped when the session it was waiting for is gone, and not sent when the agent runs on one of the five CLIs that never come to rest between turns, which are Amp, Codex, Grok, OpenCode and Pi. That last one is kept and shown rather than thrown away or parked in a queue that would never move, and it goes out when you say so. Stopping a thread, changing who is in a room, or writing a new message of your own drops whatever had not gone out yet, each with its reason, instead of waking an agent later for a conversation nobody is in any more',
      'An agent that never starts its task says so instead of looking busy. One came up, was given a session, was marked as working, and sat at an empty prompt for hours with nothing to do: from the outside an agent that is stuck and an agent that is thinking looked exactly alike, so nobody thought to check. If a session has not begun its work ten minutes after it starts, it is now marked as failed with a note saying the task never ran. The card on the Agents page changes with it, anything waiting on that agent is unblocked, and whoever asked for the work is told. Ten minutes because a normal start takes under two seconds but a start against a server that accepts and never answers can take several minutes and still be perfectly healthy, and calling a working agent broken would be the worse mistake',
      'The orders that went missing were being done all along. Every agent used to ask Tars whether it was running before it reported anything, and that question had a time limit on reaching the app but none on hearing back. Against a port that answers the knock and then says nothing, which is what a second Tars, a leftover process or a sandbox on the same port looks like, the question simply never returned. It is the first thing an agent does when it starts, so it never got as far as telling Tars which session it was, and everything it said from then on was filed under nobody and thrown away. The task ran, the work happened, and the result went nowhere. The question is gone rather than hurried: putting any time limit on it only moves the failure, because an app that is merely busy answers a moment late and the agent would skip telling Tars who it is entirely. Reporting in is now the first thing it does, and that arrives the instant it is sent however long Tars takes to reply',
      'The CLIs Tars runs no longer update themselves mid-session. They were swapping their own binary underneath a running agent, so a task could finish on a build it did not start on, and offering a restart that is not yours to perform. They also announced the check every half hour, which for an idle agent is the only thing it ever prints, so the terminal history Tars keeps filled with update notices and the actual work scrolled away: sixteen agents here had nothing else left. Updating by hand still works exactly as before, in that terminal, when you ask for it',
      'Amp joins the CLIs you can put an agent on, alongside Claude Code, Codex, Gemini, Grok, OpenCode and Pi. It is Sourcegraph\'s coding agent, you need its amp binary installed for it to show as ready, and an Amp account to sign in with. There is no model to choose: Amp picks its own mix of open and frontier models for each request, so the model row says Default and stays out of your way',
      'Hermes is connected again, and it reconnects on its own the next time Tars starts. You were signed in, the connection test was green, and Schedules still refused to read you while Chat said nothing for a week. The gateway signs a session out by sending its cookie back empty, except it sends it back as two quotation marks, and two quotation marks are not empty: Tars filed every sign-out as a real session and handed it straight back on the next call, so the nine cookies in the jar had all been two characters long since the first of September. Tars kept saying the session was alive because it only ever looked at the names of those cookies and never at what was inside them. The value is read now, an expiry in the past or a lifetime of zero clears a cookie the way it is meant to, the two secure-prefixed copies are no longer kept on a plain http connection where a browser would refuse them anyway, which is what turned three cookies into nine, and a jar that has already gone bad is emptied when the app starts, so there is no file for you to go and find',
      'The Hermes connection test spends a real request now. It used to knock at the door without signing in, and a gateway that redirects anyone who knocks read as reachable, so the button was green while every call that mattered came back refused. It tells the three apart now: reachable and signed in, reachable but signed out, and not reachable at all. Chat drives Hermes through that same door, which is why your fleet worked all week and nothing ever came back to you about it',
      'Honcho stops connecting to nothing. You could paste a valid API key, see it accepted, and get not one memory back and not one error either, which is the state an install has been sitting in. Honcho turns down every call that does not name a workspace, none of its thirty one tools asks for one, and the one command that would list them answers with a server error, so no agent could work it out. Memory Backends has a Workspace ID field now, you take the value from your Honcho dashboard, and Tars sends it on both roads: the one your CLIs use and the one the Brain page uses. Leave it empty and nothing changes',
      'Your agents can reach the tools you installed for them. Tars builds the list of folders it hands an agent, and four places were building their own version of that list which had quietly drifted apart, so where a binary was found depended on which of the four had asked. Two of them, the Google Cloud SDK and MiniMax, were missing from all four and had never once reached an agent. An agent that cannot find a tool does not say why, it just works around it, which is why this was never reported and only ever felt like an agent being slow or stubborn. The four read one list now',
      'A terminal panel can show you the conversation instead of the screen. The panels only ever looked like they kept a history: what they replayed was the raw picture stream of a full screen interface, frames drawn at one size stacked on frames drawn at another in a buffer that never clears, and a fifth of the lines above the fold carried two texts written over each other. The live terminal is left exactly as it was, because holding the screen is what a terminal does with a full screen application. Beside it there is now a history view, which reads the journal Claude Code writes line by line: one row per turn, the date named on the row where the day changes so two agents running for a week are never confused, and tool calls kept to a single dim line so machinery never reads as an answer. It is a snapshot and it says so, with a refresh button, rather than a list that shifts under you while you read it. The fifteen providers that run on the claude binary have it, and the six that write no such file say so instead of showing you an empty list',
      'Your terminals no longer type into themselves. A terminal answers questions the program inside it asks, which terminal it is, where the cursor sits, whether the mouse is armed, and those answers come back through the same door as your keystrokes. Every panel that forwards what you type was forwarding them too, so they landed in the shell as though you had typed them, a line reading [>0;276;0c sitting in front of your own command. Nine terminals did it: the agent panels on the Dashboard, the tray, the quick terminal, the agent dialog, the project shell on the Projects page, and the four install windows. The rule meant to remove those answers knew one form of them and not the other, so it cut the longest one in half and left its first five characters in place, and it carried a loose rule that matched anywhere, which is why a 276;0c typed by hand disappeared out of a line you were writing. Answers are matched whole now, from the escape character to the last one, or nothing is removed at all: a rule that can match half a sequence manufactures the fragments it was meant to delete. What you type arrives as you typed it, including text that happens to look like an answer',
      'A second Tars beside your own no longer writes into the first. The sandbox hands a test copy its own agents, its own settings and its own port, and said as much in its own description, but the small scripts your agents run to report in had the address of the main app written into them, all fourteen of them. A test agent therefore sent its status, its output and everything it observed to the app you actually use. It happened twice in one night here: six agents of a real project were shown as finished while nothing of theirs had run, and the test copy they belonged to sat there looking silent. Those scripts now follow whichever Tars started the agent, and fall back to the usual address only when no Tars did, which is what happens when you run the CLI yourself in a terminal. Nothing changes for a normal install, where the two are the same app',
      'Saving on the Settings page stops undoing what you never touched. The screen sent back its whole picture of your Claude settings, the one it took the moment you opened the page, and the app merges whatever arrives, so every value in that picture won even where you had gone nowhere near it. The hooks are the case that hurt: open Settings before Tars has written them and the picture carries the empty list a missing file reads as, then press Save and that empty list lands on top of the real ones. The page sends only what you changed now, and the app separately refuses an empty hook list or an empty permission block laid over a full one. The co-authored-by switch is why it took both: it is a yes or a no, so a stale no and a chosen no are the same thing seen from the other side, and the only way to settle it is to leave the answer out of the message when you did not touch the switch',
      'The Usage page says when its figures are missing something. Tars prices your agents by replaying the transcripts Claude Code writes, and a file it could not open contributed nothing while the total still presented itself as the total: the one error that looks like good news, because it arrives as a smaller bill rather than as a gap. When a transcript cannot be read the page now says how many, next to the title, and the figures stay correct for everything that was read.',
    ],
  },
  {
    id: 36,
    version: '1.6.18',
    date: '2026-08-27',
    updates: [
      'Chat stops repeating itself. It settled on one sentence about a sidebar width and said it a hundred and seventy eight times over a day, including straight back at you every time you wrote to it, while thirty agents worked on other things. It was copying the example sentence out of its own instructions, and then copying itself because the last twenty four turns it was shown were all that sentence. The example is a blank to fill in again rather than a finished sentence, a check-in that repeats the one before it is dropped while it is still recent, and a run of repeats is collapsed to one line before Chat is shown its own history, so a conversation that is already stuck comes back on its next message with nothing deleted from it. Telling you an agent is still blocked half an hour later is news rather than a repeat, so that still reaches you, and a question you ask always gets an answer even when the answer has not changed',
      'A question in Chat always comes back with something. When Hermes answers with the blank reply format instead of a real answer, which happens when the model the gateway is running does not follow it, Chat used to drop the answer and leave you with an error and an empty page. It now tells you what happened and that choosing a stronger model for Chat is what fixes it. The blank answer itself is still kept out of the conversation, and an automatic check-in that comes back that way is still dropped in silence rather than filling the thread',
      'Chat looks at the fleet as it is now before it answers. The snapshot used to open the prompt and a day of conversation followed it, so the oldest thing in view was the freshest. It now reads last, right before your question, and is labelled as the current state',
      'The automatic check-ins are about things that need you. An agent finishing is no longer one of them: it is the ordinary end of work, it is most of what happens once a fleet is thirty agents, and whoever asked for that agent is already told directly. An agent blocked on a question, an agent that errored, and an agent running far longer than usual still bring you a message',
      'Hold Option and click to put the cursor where you clicked. It has been there the whole time and nobody knew: it is the reason Tars will not hand the mouse to the CLI, because a terminal that gives the mouse away loses the wheel, the selection and this at once. Correcting the middle of a long message no longer means holding an arrow key down, and the same move measured four hundred and sixty two presses when it was counted',
      'Terminal text sits on the grid it is measured on. The app puts a hair of tracking on its own text, about a seventh of a pixel per character, and the terminals were inheriting it: xterm sizes a cell by laying out thirty two characters in a hidden span, so the cell came out that hair wider than the glyphs it went on to draw, and the gap piled up along the line, six pixels by column forty and thirty across a full width panel. The tracking belongs to the app now rather than to the page, so the terminal measures the font as the font is. Terminal text is a touch tighter than it was, and the column count Tars hands the CLI is right at any width, whichever way a future xterm decides to measure',
    ],
  },
  {
    id: 35,
    version: '1.6.17',
    date: '2026-08-27',
    updates: [
      'The terminal mouse change from 1.6.16 is rolled back. Letting the CLI take the mouse was meant to make a click place the cursor in a long message while Tars kept the wheel scrolling for you, and it went wrong: the scrollback stopped moving at all, full screen no longer helped, and output came back broken across lines and unreadable. The terminals behave as they did in 1.6.15 again, so scrolling and selection work and a click still does not move the cursor. The proper fix is being worked on',
    ],
  },
  {
    id: 34,
    version: '1.6.16',
    date: '2026-08-25',
    updates: [
      'An orchestrator answers a question instead of turning it into a half hour job. Its instructions only ever described handing work to one agent or to several, so the smallest thing it could do with a passing remark was open a task, and it often opened three. It now sizes the response before it reaches for anything: reply itself, one agent, or several only when the pieces are genuinely independent, and when it is torn between two it takes the smaller. Real work spread across several areas still goes out in parallel',
      'An orchestrator is allowed to talk. It was told it was an agent manager only, which stopped it explaining anything, checking a fact in the code, or responding to feedback without delegating first. It still does not write the project code, that belongs to its agents, but answering and reporting are now its own work. The same applies from Telegram and Slack, where a question used to start with "Looking at available agents..." before anyone had established that an agent was needed',
      'You can click into what you are typing in a terminal. A long message could only be corrected with the arrow keys, because Tars was refusing the mouse on the CLI\'s behalf: that was the only way it had found to keep the wheel and the selection working, since a terminal that hands the mouse over loses both. All three work at once now. Clicking places the cursor, the wheel still moves the scrollback because Tars scrolls it itself rather than letting it be sent to the CLI, and holding Option while you drag still selects text',
      'Scrolling a panel stops breaking until you toggle full screen and back. What broke it was one sequence slipping through the old refusal, after which nothing ever put the mouse back; going full screen only helped because it threw the panel away and built a new one. Nothing is refused any more, so there is nothing left to slip through. The same handling now covers all nine terminals in the app rather than five of them, which is why some windows had the problem and others did not',
      'Your agents stop being handed another project\'s rules. Tars gives every agent it starts a short set of working habits, and when Tars was run from a checkout it sent them its own development rules instead: draw the screen in a design file first, do not touch these folders, open the pull request against this repository, run these build checks. Agents on unrelated projects were following all of it, which is why some of them kept reaching for tools their project had nothing to do with. They now receive the working habits only, and your project\'s own instructions always come first. An install that has the wrong file already repairs itself the next time Tars opens',
    ],
  },
  {
    id: 33,
    version: '1.6.15',
    date: '2026-08-25',
    updates: [
      'You can rename an agent. Editing one had no name field at all, so it kept whatever it was called the day it was created; the field sits at the top of the panel and, left empty, still falls back to the generated name it shows you',
      'Changing the folder a team works in sticks. The pre-selected default arrived from disk a moment after the window opened and overwrote the folder you had just picked, so the whole team was deployed into the wrong project',
      'The model list in a team is wide enough to read. It was only as wide as the column it hangs off, about a hundred and thirty pixels, which left every model name cut to a few characters',
      'An agent can be deleted from the Agents page. The card offered open, start, stop and edit and nothing else, while the Dashboard had been saying for two versions that deleting for good lives here. The confirmation names the worktree that goes with it and counts the uncommitted changes in it, and says that commits on the branch are kept',
      'The card on the Usage charts stays inside the panel. On the last days of the month it ran off the right, which pushed the page sideways and hid the number you were pointing at; the page can no longer scroll sideways at all',
      'The local server no longer goes silent when its port is already taken. It retries instead of giving up for the life of the process, which used to leave the hooks, the seven MCP servers and delegation dead while the window looked perfectly healthy',
      'Chat answers instead of repeating the same placeholder line every five minutes. One reply came back as the blank example Hermes is shown rather than an answer, and because it was written into the conversation every turn after it copied what it had just read; a reply like that is now refused before it is recorded, and one already in the conversation is never quoted back to Hermes again, so an install that was stuck answers properly on the next message without anything being deleted from it',
      'The replies that already came back empty stop reading as messages. A run of them folds into one line that says how many there were, in the place they happened, and opens if you want to read what actually arrived. Nothing is removed from the conversation, and a line you typed yourself is never folded',
      'Sending a suggestion Chat proposed always sends exactly what you were shown. Tars now looks the proposal up in the conversation before it writes anything to an agent, and sends the wording and the target that are stored there rather than whatever the screen hands back, so a suggestion that came from one of those empty replies is refused with a line saying why instead of going out',
      'An orchestrator is told when the agent it dispatched has finished, without having to ask. Delegation only went one way: Tars knew an agent had reached a result and not who was waiting on it, so an orchestrator had to keep checking or arm a watch loop by hand, and the day nobody armed one a whole review finished with the thread stopped dead. The news now arrives on its own, and it waits if the orchestrator is mid-task rather than interrupting it, arriving as one message the moment it is free',
      'Waiting on an agent works past five minutes. The wait was a single request that the network layer under Node cuts off after exactly five minutes of silence, and since a long wait is silent by definition, every wait over a real piece of work died as a connection error instead of returning the result. It is now made of short waits back to back, so it lasts as long as it was asked to and a dropped connection costs one of them rather than the whole wait',
    ],
  },
  {
    id: 32,
    version: '1.6.14',
    date: '2026-08-24',
    updates: [
      'A Daily messages chart on the Usage page. Tokens per day cannot tell one long turn from forty short ones, and this can: hovering a day shows how many replies each model sent. A reply counts once even though a transcript writes one response as several lines',
      'The Chat message box follows the drawn design. The text no longer sits in a box inside a box: it writes straight onto the composer, and the composer border picks up the accent while you are typing in it',
      'The model, its provider and the reasoning effort are one control instead of three. It reads model and effort at a glance, and opening it offers all three, which is the submenu inside the submenu that was asked for',
      'Attach lost its border. It acts on the message rather than setting a property of it, so it is no longer shaped like the pickers next to it',
      'Send steps back while a turn is in flight. It says queue and stops being the primary button, because pressing it then does something different',
    ],
  },
  {
    id: 31,
    version: '1.6.13',
    date: '2026-08-24',
    updates: [
      'Chat talks to Hermes live instead of through a scheduled job. Every message used to be delivered by rewriting a cron job, triggering it and polling for the run, which took about thirty seconds; the gateway has a real conversation and Tars now uses it, so a reply lands in about nine. The old path is still there and takes over on its own if a gateway will not open a live connection',
      'Chat no longer creates a scheduled job it never needed. One is only made if the live connection cannot be used',
      'A reasoning effort control beside the model in Chat. It sets what your gateway runs at, so it applies to everything using that gateway rather than to one message',
      'The Chat composer is rebuilt around what it now carries. The text box takes the full width and send moves to the end of the control bar, beside attach, the provider, the model, the effort and the cadence, instead of being wedged against the text',
    ],
  },
  {
    id: 30,
    version: '1.6.12',
    date: '2026-08-24',
    updates: [
      'You can attach images and files to a Chat message. They upload to the gateway and the message names their paths, so Hermes opens what it needs with its own file tools rather than being handed a wall of base64. Attached files show as chips under the message, and stay with it if you queue it behind a reply in flight',
      'The loading mark fills instead of blinking. The top-left square lights and stays lit, the one to its right joins it, and so on to the end of the row and down to the next, until the mark is whole and it starts again',
      'You can write while Hermes is answering. The box was disabled for the whole thirty seconds; what you write now queues, shows in the thread as queued, and goes as soon as the reply lands',
      'Pausing stops a reply that is already being written. It only set a flag the next check would read, so the answer you had asked it to stop still arrived minutes later, which was worst when the turn was stuck and pause was the obvious thing to press',
      'The Chat message box is a proper box: the text sits in its own bordered field with room in it and send is beside it, rather than 13px text on a 19px line in a bare card',
      'A Daily tokens chart on the Usage page. Hovering a day breaks it down per model into what went in, what came out and what was cache, which is the shape of the bill: cache reads outweigh output by three orders of magnitude and a single daily total hides which model is spending',
      'Builds clean up after themselves, keeping the last three. Nothing ever deleted the packaged versions, so the release folder had reached 6.3GB',
    ],
  },
  {
    id: 29,
    version: '1.6.11',
    date: '2026-08-24',
    updates: [
      'A message you send in Chat stays there when you leave the page and come back. It is written down the moment you send it now, not thirty seconds later with the reply, so anything reading the conversation in between sees it',
      'Opening a picker no longer drops the caret into a search box you did not click. It focused the filter on open, which in the message card reads as being pulled out of your own sentence mid-word',
      'The mark that shows while Hermes is answering is legible. At sixteen pixels its cells were under four pixels each with sub-pixel gaps, so the travelling square read as a smudge',
      'The Chat message card sits on the raised surface, so it reads as the thing you type into rather than another message. It had been given the same surface as the messages above it',
    ],
  },
  {
    id: 28,
    version: '1.6.10',
    date: '2026-08-24',
    updates: [
      'A message you send in Chat appears the moment you send it. It only showed up when Hermes answered, about thirty seconds later, because the backend records your turn at the end of the round trip',
      'Leaving the Chat page no longer hides a reply in progress. The turn always ran in the background and the history was always kept; what was missing is that coming back looked like nothing was happening',
      'The Chat message box was rebuilt. It had grown by accretion: no room around the text, three pickers and a button crammed onto one line, and a placeholder against the edge',
      'Tars notices when you change a model in a terminal, or when an agent changes branch. Neither was ever read back, so the card kept saying the old one and, worse, the next restart rebuilt the command from that stale record and put the old model back. The session wins now',
    ],
  },
  {
    id: 27,
    version: '1.6.9',
    date: '2026-08-24',
    updates: [
      'The loading mark travels one square at a time again. It rested lit instead of grey, so the whole thing started orange and went out square by square, and the lit window was longer than the gap between two squares so several glowed at once',
      'The Chat message box grows with what you write, up to eight lines, then scrolls. It was two lines whatever you typed. The line under it is gone, and the text sits further from the edge',
      'Opening a picker in Chat takes the caret out of the message box. It kept it, so typing went into the message instead of the picker',
      'Agents are grouped by project in the Chat fleet list and in Logs, the way Review already did',
      'Every dropdown in Settings is the app\'s own now. The default provider, the terminal theme and font size, the Tasmania model and the default Claude model still opened the operating system\'s menu, which ignores the app\'s colours entirely',
      'Hovering a bar on the Usage chart shows a card in the app\'s design rather than the grey system tooltip',
    ],
  },
  {
    id: 26,
    version: '1.6.8',
    date: '2026-08-24',
    updates: [
      'An update that will not install says so instead of sitting on "Restarting" forever. The handover was called and never checked, so a refusal left the panel spinning with nothing to do about it. It now explains what happened and offers the download.',
      'A new version no longer takes over the middle of the screen. The row in the menu appears and stays until you install it, and the panel opens when you click it.',
      'A second thing the Chat agent can be allowed to do on its own: restart an agent that has errored. Like the first, it is off until you turn it on, and it waits a minute so it does not race the error it is answering.',
      'The creation screen says whether orchestrator mode is actually enforced on the CLI you picked. It is on Claude and the thirteen providers that run it, and on the five with their own syntax there is no verified way to take the editing tools away, so the switch guides rather than stops. It used to imply the same thing everywhere.',
    ],
  },
  {
    id: 25,
    version: '1.6.7',
    date: '2026-08-24',
    updates: [
      'An orchestrator can no longer quietly do the work itself. Thirteen of the providers run the same Claude binary and none of them applied the orchestrator restriction, so an orchestrator on DeepSeek, Venice or Ollama Cloud could edit files directly. All fourteen now share one list, and the built-in subagent tool is on it: spawning one looks like delegating and is not, because that work never reaches the specialist agent and never appears in your fleet',
      'Agents call back into the app that started them. The bundled tools were pointed at a fixed port, so an app started on any other one had its agents delegating into whichever copy owned that port',
      'The Chat agent remembers the last day. It saw a snapshot of where everyone was and nothing about how it had been going, so it could say an agent was waiting and never that it had restarted three times on the same task',
      'You can let the Chat agent act on its own, for the things you say in advance it may do. It starts with one: nudging an agent that has been waiting on you for more than five minutes, never one blocked on a permission dialog. Everything else still waits for you, and anything sent automatically says so in the chat and names the rule that allowed it',
    ],
  },
  {
    id: 24,
    version: '1.6.6',
    date: '2026-08-24',
    updates: [
      'Removing a panel from a project board takes it off the board instead of destroying the agent. It keeps running, keeps its worktree and stays reachable everywhere else, and the tab strip says how many are hidden and puts them back. Deleting for good is still there, in the same menu, with its own wording and its own warning',
      'The Chat agent reacts to what happens instead of waking on a timer. An agent starting, pausing for you or finishing now wakes it directly; it waits for the fleet to settle first and never looks more than once a minute, because every look is a real run. The timer stays as a safety net for the things no status change announces',
      'The loading mark runs one square at a time, left to right along each row and down to the next, instead of lighting a whole diagonal at once. It is the only waiting animation left in the app: the row of pulsing squares that used to appear on a slow load and beside the Logs search is now the same mark',
    ],
  },
  {
    id: 23,
    version: '1.6.5',
    date: '2026-08-24',
    updates: [
      'Tars looks for a new version while it is running, every half hour, not only in the five seconds after it starts. Left open for days, it never told you there was an update. A banner you dismiss stays dismissed until there is a newer version than the one you dismissed',
      'Hermes replies read like writing instead of like a file. Bold, italics, inline code, bullets and code blocks come out formatted rather than showing their asterisks and backticks. The formatting is built as text, never as markup, so a reply quoting a log line cannot run anything',
      'The Chat message box is quieter. The model, provider and cadence pickers were three bordered boxes with the same weight as the send button, so the row read as four controls fighting each other. They sit on a hairline under what you type, as plain labels, and send is the only thing with colour',
      'Kanban puts "New task" beside the title like every other page, instead of on a row of its own underneath',
      'The sidebar stops leaving Dashboard lit while you are somewhere else. In the packaged app the page you are on could not be read from the address, so every entry was compared against a value that always said Dashboard, and two entries lit at once',
      'A reply Hermes sent in a shape Tars could not read no longer arrives as raw JSON in the chat. A truncated answer now shows what it managed to say, and one that says nothing readable says that instead of printing an object at you',
      'Header buttons are the same size on every page. Kanban and Chat were using the small 26px control where Agents, Vault and the rest use the standard 32px one',
      'The Chat conversation reaches the fleet rail on a wide window. It was capped at 830 pixels, which is exactly the room beside the rail at the default size and a hole at any larger one',
      'The message box centres what it is asking you for, and the cadence menu drops a line about Hermes runs that was not yours to worry about',
      'The Git setting says what it actually does: it is Claude Code\'s own co-authored-by trailer, kept in its settings file, so it does not reach Codex, Gemini, Grok, OpenCode or Pi',
    ],
  },
  {
    id: 22,
    version: '1.6.4',
    date: '2026-08-24',
    updates: [
      'Updating Tars no longer throws away what your agents were doing. Restarting the app kills every terminal, and each agent used to come back with an empty conversation: it knew which session it had been in, but that was deliberately forgotten on load. It now remembers separately, and picks the conversation back up on the first start after a restart. Pressing Start yourself still means a fresh conversation, as it always has',
      'An agent whose transcript has been cleaned up starts fresh instead of refusing to start. Resuming a session that is no longer on disk makes the CLI exit, so Tars looks for the file before asking for it',
      'The custom OpenAI-compatible endpoint sits with the other providers in Settings instead of in a section of its own. It is a provider like the rest, with two more fields',
      'In Chat, the model, provider and cadence pickers moved down beside the box you type in, where what you are about to send is. They were on the title row, which crowded it',
      'Choosing a model in Chat now actually changes what answers. Tars was pinning the model on its scheduled job, which the gateway accepts, stores and then ignores, so the choice looked like it had taken and nothing changed. It sets the gateway model instead, which is a gateway-wide setting: the control says so, and it reports back when the gateway refuses',
    ],
  },
  {
    id: 21,
    version: '1.6.3',
    date: '2026-08-24',
    updates: [
      'Any OpenAI-compatible vendor is reachable now, not just the ones Tars ships a provider for. A new "Custom (OpenAI-compatible)" provider takes a base URL, an optional key and a model, and Tars translates for it the same way it already does for Venice',
      'Ollama Cloud joins local Ollama as its own provider. It is a different product from the server on your machine, billed separately, reached at ollama.com with your own key',
      'The Venice translator is no longer Venice-specific under the hood. One local bridge now serves every OpenAI-only vendor, addressed by a path segment rather than a port each, so adding the next one does not mean adding another server',
      'A base URL you type for a custom vendor can no longer point at your cloud provider\'s metadata address, which would have been called with your stored key attached. Your own machine and your own network are still allowed, because pointing this at a model running on your desk is the whole reason it exists',
      'The model list tells the truth about what is newest. It read the vendor\'s alias names literally, so it announced the 4.5 models as the latest long after 5 had shipped, and it now sorts by release date, marks an alias as one, and follows new releases without an update to Tars',
      'Deleting an agent says what goes with it. It removes the agent\'s working tree, and if there are uncommitted changes in it, it now says how many before you confirm. Commits on its branch are kept either way',
      'The Chat agent is yours to configure. Pick which model and which provider Hermes answers on, from what your gateway can actually run, and how often it looks at the fleet, anywhere from every minute to every six hours. It was fixed at five minutes on whatever model the gateway happened to have selected',
      'Panels can be arranged inside a project, not only on a custom tab. Dragging one used to do nothing at all there: the handle was live, the panel moved, and it sprang straight back. Each project keeps its own arrangement, and an orchestrator starts in the top left',
      'The project tabs can be dragged into the order you want, and they stay there. Their order used to be whichever order the agents happened to be in, so it changed under you every time an agent was created',
      'Obsidian is a memory your agents can read and write. Point Tars at your vault in Settings, and it becomes searchable alongside the project\'s own notes. Agents write into a Tars folder inside the vault, never into a note you maintain',
      'Creating a template offers every skill you have, with a filter for finding one. The list was a small box that showed the first few and scrolled the rest out of sight',
      'The working trees in Review are grouped under the project they belong to, instead of one flat list of branch names',
      'The zoom on the Brain map follows your pointer. Pinching zoomed at the same spot wherever the mouse was, so getting to a corner meant zooming and then scrolling back',
      'Vault puts its "+ Document" button beside the title like every other page, Logs no longer shows a terminal prompt glyph as a leftover logo on an untouched page, and the options rows when you create an agent share one right edge instead of three',
      'An available update has a permanent home in the menu, just above What\'s New, for as long as one is waiting. The update panel is dismissible, and dismissing it used to hide the update until the next launch',
      'Settings shows the animated mark while it loads, like every other page. It showed grey placeholder bars in the shape of a list, which is not what a settings page is',
    ],
  },
  {
    id: 20,
    version: '1.6.2',
    date: '2026-08-24',
    updates: [
      'Creating an agent is one screen instead of four steps. The folder, the CLI and the task are visible at once, the task is the field you land on because it is what you came to type, and everything else sits behind a single row that reads out what it currently holds. Teams are the same screen with a switch, and their members are a table you read across, so two of them on the same model or one without its own branch is obvious at a glance',
      'Every provider is selectable again. The redesigned screen showed four, and the thirteen others, including Ollama and Venice, could not be picked at all',
      'Templates have a button of their own on the Agents page. They used to open from a row inside the old first step, which the redesign removed',
      'Agents stop needing to be asked twice. Handing a task to an agent that pauses for confirmation gave up after one nudge, and two requests arriving at the same moment for the same agent each started their own terminal, with one of them silently orphaned. That could happen from a double-clicked Start, or from an orchestrator delegating while you resumed the same agent yourself',
      'Four panels stayed dark on a light page, the terminal panel menu announced itself as three dots to a screen reader, and the read-only permission fields now name the file you would edit',
      'Two settings controls that said only "Not wired yet" do their job, and the two that genuinely cannot point at the page that can',
    ],
  },
  {
    id: 19,
    version: '1.6.1',
    date: '2026-08-24',
    updates: [
      'Saving any setting no longer wipes the ones written elsewhere. The screen sent its whole snapshot of your settings back on every change, so a value the app had updated on its own in the meantime, the Slack channel it learned from an incoming message, the Telegram token it had just regenerated, was overwritten with whatever the screen had loaded minutes earlier. It sends only what you actually changed now',
      'Brain says what your gateway told it. gbrain is registered on your Hermes gateway at its own localhost, which this machine cannot reach, and the page now says exactly that instead of leaving an empty field labelled "not enabled". It also shows that your gateway has no memory provider selected, with the ones that are ready to turn on',
      'The name you type when creating an agent is the name it gets. The field wrote to state nothing read, so every agent ended up with the generated name instead',
      'Creating an agent no longer loses what you typed when it fails. The form cleared itself the instant it asked, without waiting to hear whether it had worked, so a failure left a blank dialog and no explanation',
      'An agent can be deleted from its project tab, not only from a custom one. The control was there and hidden',
      'An agent whose record has no skills list starts again. That list is only written by versions that had it, so a restored backup or a hand-edited file made the agent refuse to run, with the reason buried where nobody could see it',
      'The board stops redrawing every pane when one agent changes status. With twenty agents that was twenty terminals re-rendering for one status square',
    ],
  },
  {
    id: 18,
    version: '1.6.0',
    date: '2026-08-24',
    updates: [
      'Chat: a Hermes agent that watches every agent in every project and reports to you. It tells you what they are doing, which decisions are in flight, and which ones are stuck on you rather than on the work, and it will challenge an agent that is going in circles. It speaks to you before it speaks to any of them: an instruction to an agent arrives as a proposal naming the agent, its id, its project, its CLI and the exact words, and nothing is written until you press send. The target is resolved from the live fleet at the moment you confirm, never from what the model remembered, so it cannot write to the wrong terminal',
      'Two more providers: Ollama, for whatever you run locally, and Venice AI. Ollama needs no key, just a running server; Venice has no Anthropic-compatible endpoint at all, so Tars now translates for it locally rather than leaving it unreachable',
      'Usage: every bar in the daily chart was labelled with the wrong day. The label came from your local calendar and the money was looked up by the UTC one, so anywhere east of Greenwich each bar carried the previous day\'s spend under the current day\'s name, and the "latest day" card (which looks its own day up) disagreed with the bar right next to it. That is the $254.14 against $446.20. Both sides now key on the same local day',
      'You can scroll and copy in a terminal again. Claude Code re-arms mouse tracking on almost every redraw, and once xterm honours that it disables selection and takes the wheel to encode it as a mouse report, so the pane could neither scroll nor be selected. The stored output replayed when a pane mounts carried the same sequences, which armed it on sessions that had exited days ago. Tars refuses those modes now, and Cmd+C copies the selection, which nothing did before',
      'The launch screen actually appears. It decided whether to show itself while rendering, by reading session storage, so the exported HTML had no splash and the first browser render did: React saw two different trees, threw a hydration error on every launch, and the animated logo you had never seen was the casualty',
      'The effort level can be changed. The ladder moved to the Model step but the wizard kept handing the controls to the Task step, which had stopped drawing them, so the chips looked live and every click did nothing, in create and in edit alike',
      'Orchestrator mode is reachable. Same wiring failure, worse consequence: the toggle was declared on the Tools step, passed to the Task step, and drawn by neither, so no agent could be made an orchestrator from the interface at all',
      'The Tools step is worth using. It was one flat wrap of every installed skill, name only, 84 of them on this machine, which meant reading the whole cloud and knowing in advance what each one did. It now has a filter that matches names and descriptions, a line of explanation on every row, and what you have picked stays visible as chips you can remove without scrolling back',
      'Pickers inside a card no longer open a stock macOS menu. A native select draws its popup through the operating system, so it ignored the palette entirely: white rows over a dark modal. Model, provider, project, CLI binary and the template fields all open a themed panel now, and it keeps what the native control gave for free, arrows to move, Enter to choose, Escape to cancel, plus a filter once a list runs past a dozen entries',
      'Kanban is the Hermes board and stops asking. The Hermes/Local switch is gone, along with the remembered choice that could silently reopen a different board. A gateway that will not answer now says which address it tried and why, instead of printing a raw ECONNREFUSED from the IPC layer',
      'Schedules can be edited. The gateway has accepted edits all along and Tars simply never called the route, so a job could only be run, paused or deleted. Name, expression, prompt and enabled state are all editable, and a rejected expression comes back with the gateway\'s own list of the forms it accepts',
      'Settings stops asking you to sign in to a gateway you are signed in to. Whether the session is live is the gateway\'s answer, not local knowledge, and the page never asked: it only found out when you pressed Test. It asks once on open now, and shows nothing either way until the answer lands',
      'Brain reads Hermes\' own memory. It was asking for those files by a relative path and the gateway answers "Path must be absolute", so every read failed and was then flattened into "0 files readable": a healthy green badge over an empty source. The same silent failure meant no agent ever received Hermes memory in its prompt. A backend you have not set up now says so and where to set it up, instead of "not enabled"',
      'The agent map in Brain can be zoomed. It was laid out at a fixed size that grows with the number of agents inside a plain scroll box, so past a couple of dozen there was no way to pull back and see the shape of it. Buttons, a fit control, and Cmd or Ctrl with the wheel',
      'Projects has one add button instead of two. The full-width row under the grid called exactly the same thing as "+ Project" in the header',
            'Security: the app could be made to read any file on your disk. Serving the interface from a packaged bundle skipped the containment check its sibling has always had, and a percent-encoded path slipped past the parser, so a note in your vault holding one crafted image link read your provider keys the moment you opened it. Closed, with the exact vector pinned by a test',
      'Security: agent output no longer leaves this machine with your keys in it. What goes to Telegram was the raw terminal buffer with the colour codes taken out, and an agent\'s environment holds the real key for whichever provider it runs on, so one printed configuration was enough. It is filtered now',
      'Security: the Telegram tool refused ~/.env and nothing else, so a .env inside any project, which is where they actually live, could be sent. Removing an MCP server built a shell command out of its name, three times over, each one directly under the comment explaining that the add path beside it had been fixed for that. And two hook routes never checked which session they came from, so an agent you had moved on from could still raise a notification saying it needed permission',
      'Usage, Projects, Settings and Extensions no longer dead-end on a bare error with no way back. Extensions can also tell a dead network from an empty search, instead of offering to clear filters on a list that never loaded',
      'The monthly budget is set on the row that shows the spend it caps. The old text sent you to a Settings field that was never built',
      'Vault search stopped crashing on ordinary words. A slash, a percent sign or an apostrophe went into the index as syntax, so "Q1/Q2", "100%" and "quote\'s" each threw instead of searching. Deleting a folder also asks twice now, like deleting a document already did, since it takes the subfolders with it',
      'The Kanban board showed nothing at all: the gateway sends its columns as a list and the page read them as a lookup, so every column came back empty however many tasks were in it. It is also no longer read-only. Cards can be opened from the keyboard',
      'The daily figures are computed once per transcript instead of once a minute forever. On this machine that is 414ms of frozen interface every minute, gone, with the same numbers to four decimal places',
      'Idle status was unreadable in both themes, below the contrast floor for text on every surface it sits on',
      'No em dashes anywhere: not in the interface, the changelog, the agent prompts Tars writes, the landing page or the repo\'s own documents',
    ],
  },
  {
    id: 17,
    version: '1.5.2',
    date: '2026-08-24',
    updates: [
      'Usage was wrong every single day, and right in total. Each day was rebuilt by taking that day\'s input and output tokens and multiplying by an all-time average rate, but cache reads are the bill (a billion of them against a million output tokens) and were not in the daily figures at all. Days came out between 83% under and 158% over; the errors cancelled across the whole period, which is why the total looked correct and hid it. Every day is now priced from its own tokens, cache included, and the day-by-day figures add up to the total exactly',
      'Schedules no longer crashes. Hermes sends a schedule as an object; the page put it straight into the markup, React refused it, and because that happens while the page is still coming up there is no error screen to catch it: you got the browser\'s "This page couldn\'t load" instead. It also reads Hermes\' real field names for the pause state now, so paused jobs show as paused',
      'You can pick any folder for an agent again. The design pass removed the browse control from the project step while leaving everything behind it wired, so you were stuck with whatever the dropdown happened to list. Choosing a folder also remembers it, so the list grows instead of making you browse again',
      'Every link with a parameter works in the packaged app: "Sign in to Hermes" from Kanban and Schedules landed on "not found", because the URL was split by hand and the query string was treated as part of the filename',
      'Opening a project starts its agents, once. You get the CLI running instead of a bare shell; switching tabs back and forth still starts nothing',
      'The start button in a terminal header is an ordinary bordered action instead of a solid orange block in every pane at once',
      'An agent asked to list agents "with all: true" no longer presents other projects\' agents as its own. They are listed separately and marked undelegatable, which is what the server already enforced',
    ],
  },
  {
    id: 16,
    version: '1.5.1',
    date: '2026-08-23',
    updates: [
      'Agents start again. Start wrote `cd <folder> && claude ...` into the terminal; if the folder had been moved or deleted, bash printed "No such file or directory", the command after it never ran, and you were left at a shell prompt with nothing saying it had failed. Tars now checks the folder first, names it, and tells you what to do about it',
      'Signing in to Hermes lasts. The session cookies lived only in memory, so every relaunch (and every crash) came back signed out, with Kanban and Schedules showing "Unauthorized" for a gateway you had authenticated against days earlier. They are now stored the same way as the API token: owner-readable only, written atomically, restored at startup',
      'Settings → Hermes: the dead field beside the Token/OAuth picker is gone. It was the token input rendered disabled under OAuth, next to a "show" control that could never reveal anything. And the picker itself is a segmented control now, like every other two-way choice in the app',
      'Usage no longer reports two different numbers for the same day. The chart was drawn from costs reconstructed out of the Claude transcripts while the "latest day" card fell back to Tars\' own ledger, and silently moved to whatever day that ledger last had. Both read one figure now, and it includes the turns transcripts never see: every non-Claude CLI, and everything run over ACP',
      'Schedules survives whatever date format a gateway sends it. One unparseable field could take the page down',
      'The updater stopped 404ing on every launch: the release was missing the metadata file it looks for',
    ],
  },
  {
    id: 15,
    version: '1.5.0',
    date: '2026-08-22',
    updates: [
      'Delegation over the Agent Client Protocol: a delegated task now returns the agent\'s answer, why the turn ended, which tools it used and what it cost. Typing a task into a terminal never told you any of that. Works on Claude Code, Codex, Gemini, Grok, opencode and pi through the same conversation',
      'Tool denial for every agent, not just Claude: risky calls arrive as a permission request Tars answers itself, so an orchestrator that must not edit files is stopped by the protocol rather than by a flag one CLI happens to support',
      'Memory reaches every CLI. Hermes\' own MEMORY.md and USER.md, full-text search over its past sessions, gbrain and Honcho are real sources now, contacted for real: the Brain page says "reachable" only when something answered, and lists the tools each backend offers',
      'A bundled memory MCP server (search, read, write, sources) is registered with every provider, and the CLIs with no session-start hook get the project\'s memory injected into their prompt so they cannot start blind',
      'Prices and model lists come from a live catalogue instead of a table baked into the release. A model shipped today appears today; a price change lands without an update. The old table had already drifted - Gemini 3 Pro was listed at a sixth of its real output price',
      'Review page: what each agent actually changed (the files, the line counts and the patch), compared against the branch it was cut from, with untracked files included instead of invisible',
      'Logs page: one search across every agent\'s output, with regex, instead of opening each terminal in turn',
      'Schedules, Review and Logs all reachable from the sidebar, and the Cmd+digit shortcuts printed next to each entry finally do something',
      'Your agent list can no longer be lost: the file is written atomically, a corrupt copy can no longer overwrite the backup, a parse failure is preserved instead of silently replaced, and terminal activity is flushed on a timer rather than only at quit',
      'Statusline costs half what it did (115ms to 49ms per render): it spawned 32 subprocesses per turn, per agent, one per field read',
      'Agents stopped re-registering their MCP servers on every boot: the check looked in the wrong file and always answered "not registered"',
      'A new icon, and the app finally packages itself as Tars rather than Dorothy',
      'The whole app redesigned: one shell on every screen, two control heights instead of nine, and the orange accent reserved for actions. The 2px orange rules that marked every active tab, row and menu item are gone',
      'Dark is the launch default again. A stale preference under the old brand\'s key was opening the app in light mode',
      'Light mode is readable: its muted text was using the dark theme\'s grey, which measured 2.6:1 on a light page (under half of what is legible). Every text colour now clears WCAG AA in both themes, solved for rather than eyeballed',
      'A launch screen and loading states: the window used to appear as a bare shell while providers were detected, which reads as a hang',
      'Security: registering an MCP server ran its arguments through a shell, so a filename could execute code in the main process at every launch. app-settings.json (which holds every provider API key) was world-readable. The bundled Telegram server could send any file on disk to any chat. A worktree branch name could place an agent\'s working directory anywhere on the filesystem. All closed, with tests',
      'A delegated task\'s waiter never woke: /run-task announced status changes on a channel /wait was not listening to, so the caller hung until its own timeout',
      'The Logs page crashed for anyone who had agents and restarted the app: it read a buffer that only exists while an agent is running',
    ],
  },
  {
    id: 14,
    version: '1.4.0',
    date: '2026-08-22',
    updates: [
      'Dorothy is now Tars: new name, new landing page, same app',
      'Opening the dashboard no longer starts your agents. A mount-time effect was resuming every idle agent that had no terminal attached, so simply navigating home launched all of them; starting is now an explicit action, and leaving and coming back never touches a running session',
      'Schedules page: your Hermes cron jobs finally have a screen with every profile in one list, run now, pause, resume or delete, refreshed while the window is visible',
      'Settings went from seventeen flat entries to six groups (General, AI & Providers, Hermes, Integrations, Extensions, Workspace). Nothing was removed, it just stopped being a scroll hunt',
      'Usage shows real money again. The page priced tokens from ~/.claude/stats-cache.json, a file most accounts never get, so every cost was computed from zero. Tars now reads the transcripts directly, drops the messages a resumed session copies forward (over half of them, which would have doubled every figure), and prices 1h cache writes at their real rate',
      'Plugin catalogue was pointing at a deleted repository and served nothing on a fresh install. It now reads the seven real Claude Code marketplaces: 685 plugins, and installing one registers its marketplace first, which is the step that used to fail',
      'Skills directory no longer truncated to 300 of its ~600 entries',
      'Your projects survive updates: they live in ~/.dorothy/projects.json instead of browser storage keyed to the app origin, and the phantom card that claimed every agent is gone',
      'Deploy Team: pick which members to deploy, add extras beyond the template, and choose the provider and model per member instead of Claude for everyone',
      'The version in Settings is the real one, read from the running app rather than a hardcoded string',
      'Security: closed command injection through the Brain page, added Origin checks on the local API, removed an unauthenticated memory endpoint, and the Hermes webhook now publishes a single path instead of the whole API',
      'Menus, dropdowns and selects are rendered by the app, not the OS, so they follow the theme; sidebar collapse and the heavy yellow rules are gone, and page titles have their subtitles back',
    ],
  },
  {
    id: 13,
    version: '1.3.0',
    date: '2026-08-22',
    updates: [
      'Hermes integration: connect Tars to your self-hosted Hermes gateway with the same four modes as Hermes Desktop (local, SSH, remote gateway, cloud), including one-click "Import from Desktop" and sign-in for cookie-gated gateways',
      'Kanban now reads the Hermes board: tasks, workers and runs stay in Hermes (its harness owns them); Tars shows the native eight columns, with the local board still one click away',
      'Incoming webhook (POST /api/webhooks/hermes) so Hermes cron jobs can drive your agents: resolve by agent name or id, with a dry-run mode for wiring it up',
      'Shared memory backends: point Tars at gbrain and Honcho and every claude-binary agent gets the same brain as Hermes; the Brain page browses native project memory alongside them',
      'Memory really is injected now: agents wake up with the project MEMORY.md plus recent cross-session activity (the endpoints the hooks called never existed)',
      'Cooperlabs redesign: tangerine on near-black, Roboto Condensed/Mono, hard corners, flat surfaces, dark by default',
      'White flash on navigation fixed at the root: RSC payloads were served with the wrong MIME type, forcing a full document reload on every first visit to a page',
      'Fonts are bundled: no more render-blocking request to Google, and the app looks right offline',
      'Agent creation is one flow: pick Agent or Orchestrator, start from a template, all in "+ Agent"; team deployment moved to "+ Team" with a per-member editor (model, effort, branch, permissions, instructions)',
      'Dashboard boards follow your project folders: custom boards removed, project tabs promoted to the top',
      'Removed for good: ClaudeMon, Support, the 3D view, Obsidian, and the unused Automations/Scheduled Tasks subsystems (~18k lines)',
      'Electron 43, Next 16.3, Node 20+, plus a Playwright suite that drives the real app and screenshots all 33 surfaces on every change',
    ],
  },
  {
    id: 12,
    version: '1.2.9',
    date: '2026-07-07',
    updates: [
      'Add Grok CLI as a new agent provider',
      'Fix Telegram small message issue: short messages are now submitted instead of left pasted',
      'Add search input to the "Add agent to board" dropdown on the dashboard',
    ],
  },
  {
    id: 11,
    version: '1.2.8',
    date: '2026-05-05',
    updates: [
      'Agent Templates: new sidebar page with 9 built-in role templates, including Frontend Engineer, Backend Engineer, Security Engineer, Code Reviewer, Tester (QA), Refactor Specialist, Docs Writer, DevOps Engineer, Product Designer',
      'Production-grade system prompts on every built-in template, grounded in patterns from Cursor, Cline, Roo Code, Anthropic Agent Skills, and the OpenAI Codex prompting guide',
      'Built-in templates are editable: your edits are saved as overrides with a "Customized" badge and a one-click reset to defaults',
      '"Use this template" → pick a project → agent created and auto-started with the template prompt; no extra setup',
      '"Save as template" action on each agent card: turn any working agent into a reusable template',
      'Clickable "+" badges on missing skills: install marketplace skills directly from a template card; the badge updates live when the install completes',
      'Import / Export templates as .json files: share role packs with your team or back up your customizations',
      'Agents list now sorts by creation time by default (newest first), so a freshly created agent appears at the top',
      'Unlimited dashboard tabs: the previous 5-tab cap is gone; tabs scroll horizontally when they overflow',
      'Ctrl+Tab / Ctrl+Shift+Tab to cycle through dashboard tabs, with focus auto-restored to the last active terminal in each tab: type straight into the agent without clicking',
      'New blank template form simplified: no more model picker; uses the provider default',
    ],
  },
  {
    id: 10,
    version: '1.2.7',
    date: '2026-03-28',
    updates: [
      'New Support page: one-time and monthly donations via Stripe',
      'Agent permission modes: Normal, Auto, and Bypass replace old toggle',
      'Agent effort level setting (Low, Medium, High)',
      'Major Usage page overhaul: model breakdown donut chart, top projects, weekly trends',
      'Multiline input support: Shift+Enter inserts newline, auto-resizing textarea',
      'Broadcast mode: send input to all terminals at once',
      'Terminal fixes: preserve content on navigation, fix fullscreen buttons, scroll-to-bottom on replay',
      'Sidebar and Kanban UI polish',
    ],
  },
  {
    id: 9,
    version: '1.2.6',
    date: '2026-03-28',
    updates: [
      'Add delay between PTY write and carriage return to fix Telegram/Slack input',
      'Add Chrome browser sharing for agents (--chrome flag)',
      'Fix CLI path detection, settings sidebar scroll, and UX improvements',
    ],
  },
  {
    id: 8,
    version: '1.2.5',
    date: '2026-03-17',
    updates: [
      'Add support of PI agent provider',
      'Add support of OpenCode agent provider',
      'Add support of JIRA self hosted domain',
      'Added macOS menu bar tray with live agent status panel',
      'Status tabs in tray: Working, Waiting for inputs, Ready to work, Idle',
      'Manage all your external MCP servers (outside of Tars) from the settings page',
      'Live task preview next to agent name when working or waiting',
      'Full-color Tars logo in the macOS menu bar',
      'Revamped agents page with improved layout and filtering',
      'Add new Status line option (in settings) to display model, context usage, git branch, session time, and token stats in live on your Claude Code terminal',
      'Custom MP3/audio file support per notification type',
      'New "Response Finished" notification toggle (Stop hook)',
      'Dedicated PermissionRequest and TaskCompleted hook events',
      'Fixed agent status lifecycle: idle on start, working only after user prompt',
      'Added pinned and favorites projects to the project page, quickly select your default project on create agent and kanban task',
    ],
  },
  {
    id: 7,
    version: '1.2.4',
    date: '2026-02-26',
    updates: [
      'Multi-provider support: Claude, Codex, and Gemini agents',
      'Provider selector in agent creation flow',
      'Memory page now shows projects across all providers',
      'Custom MCP server configuration per provider',
      'CLI Paths settings for all provider binaries',
    ],
  },
  {
    id: 6,
    version: '1.2.3',
    date: '2026-02-19',
    updates: [
      'React app preview tab in agent detail panel',
      'Live preview of react-app code blocks from agent output',
      'File watcher for .dorothy-preview/ directory',
      'Window drag regions for macOS',
      'Modular API routes for better maintainability',
    ],
  },
  {
    id: 5,
    version: '1.2.2',
    date: '2026-02-10',
    updates: [
      'Skills marketplace with community skill browser',
      'Skill installation progress terminal',
      'Link skills to specific providers',
      'Improved agent world (ClaudeMon) with NPC zones',
    ],
  },
  {
    id: 4,
    version: '1.2.1',
    date: '2026-01-28',
    updates: [
      'Vault: shared document storage for agents and users',
      'Folder organization and full-text search in Vault',
      'Kanban board with agent task assignment',
      'Auto-spawn agents from Kanban card moves',
      'Scheduler improvements with cron expressions',
    ],
  },
  {
    id: 3,
    version: '1.2.0',
    date: '2026-01-10',
    updates: [
      'Telegram bot integration for remote agent control',
      'Slack bot support with channel notifications',
      'JIRA integration for issue tracking',
      'Automations engine for event-driven workflows',
      'Super Agent / Orchestrator mode',
    ],
  },
  {
    id: 2,
    version: '1.0.1',
    date: '2025-12-20',
    updates: [
      'Desktop notifications for agent events',
      'Memory browser for Claude project memory files',
      'Obsidian vault integration',
      'Dark mode support',
      'Worktree support for isolated git branches',
    ],
  },
  {
    id: 1,
    version: '1.0.0',
    date: '2025-12-01',
    updates: [
      'Initial release of Tars',
      'Multi-agent management with persistent PTY sessions',
      'Agent creation with project path, skills, and character',
      'Terminal view with live output streaming',
      'Dashboard with agent status overview',
      'Scheduled tasks with cron support',
    ],
  },
];

export const LATEST_RELEASE = CHANGELOG[0];
export const WHATS_NEW_STORAGE_KEY = 'tars_whats_new_last_seen';
